services:
  inventory-app:
    build:
      context: .
      args:
        - APP_VERSION=${APP_VERSION:-dev}
    ports:
      - "3333:80"
    environment:
      - POCKETBASE_URL=http://pb.matzeschneider.de
    restart: unless-stopped
